name: Second Workflow (Called Workflow)

on:
  workflow_call:  # Triggered by another workflow

jobs:
  display_message:
    runs-on: ubuntu-latest

    steps:
    - name: Display Message
      run: echo "This is the message displayed by the second workflow!"
